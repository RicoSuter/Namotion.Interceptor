using Xunit;

namespace HomeBlaze.Authorization.Tests;

/// <summary>
/// Basic smoke tests to verify project setup.
/// </summary>
public class SmokeTests
{
    [Fact]
    public void ProjectReferenceWorks()
    {
        // Verify HomeBlaze.Authorization project is correctly referenced
        Assert.True(true);
    }
}
